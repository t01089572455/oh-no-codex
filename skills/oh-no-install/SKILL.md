---
name: oh-no-install
description: >
  Install Oh No Codex cooperative hooks and all oh-no-* skills into
  ~/.codex/skills. Use when user says ohno install, 安装 hooks, install oh-no
  skill, setup codex guardrails for ohno, or skill missing for oh-no-control.
---

# oh-no-install

```bash
ohno install
```

Idempotent. Also:

```bash
ohno skill install
ohno skill status
```

Ask user to open a **new Codex session** after install so skill discovery refreshes.
